<h1>Edit Wiki</h1>
<div class="row">
  <div class="col-md-4">
    <p>Blocipedia renders in Markdown!</p>
    <div class="form-group">
    <p>Collaborators</p>
    <% wiki_collaborators.user_id = @wiki.wiki_collaborators.user_id %>
    <%# this should more properly use a variable set in your controller rather than User.all - for example you might want to limit the list of possible collaborators according to some condition %>
    <% User.all.each do |user| %>
      <div class="user">
        <%= check_box_tag "wiki[wiki_collaborators.user_id][]", user.id, wiki_collaborators.user_id.include?(user.id) %>
        <%= user.name %>
      </div>
    <% end %>
  </div>
  </div>
  <div class="col-md-8">
    <%= render partial: 'form', locals: { wiki: @wiki } %>
  </div>
</div>
